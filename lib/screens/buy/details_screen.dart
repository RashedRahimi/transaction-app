import 'package:flutter/material.dart';

class BuyDetailsScreen extends StatelessWidget {
  const BuyDetailsScreen({Key? key}) : super(key: key);
static String routeName = '/buydetailsscreen';
  @override
  Widget build(BuildContext context) {
    return Container(

    );
  }
}
